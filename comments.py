import math #library module for math functions
def circle(r): #defining function that will calculate the area
    area=math.pi*(r**2) #area of circle formula
    return area #returns calculated value

r=7 #radius of the circle
print(circle(r)) #prints calculated area

